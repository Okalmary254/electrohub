from django.urls import path
# from . import views  # Uncomment and use if you have order views

app_name = "order"

urlpatterns = [
    # Example:
    # path('', views.order_list, name='list'),
    # path('<int:pk>/', views.order_detail, name='detail'),
]
